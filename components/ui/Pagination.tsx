export const Pagination = () => {
  // 仮UI（今後実装拡張予定）
  return (
    <div className="flex gap-2">
      <button className="px-3 py-1 rounded border border-border text-sm text-text">
        前へ
      </button>
      <button className="px-3 py-1 rounded border border-border text-sm text-text">
        次へ
      </button>
    </div>
  );
};
